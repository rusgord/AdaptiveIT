namespace AdaptiveIT.Models
{
    public enum AccessType
    {
        All,
        ByGroup,
        ByStudentList,
        ByCode
    }
    public enum QuestionType
    {
        SingleChoice,
        MultipleChoice,
        TextMatch,
        GraphicDragAndDrop,
        ShortAnswer,
        Ordering,
        LongAnswer
    }
}